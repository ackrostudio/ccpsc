doctype html
html(lang='es')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='description', content='A fully featured admin theme which can be used to build CRM, CMS, etc.')
    meta(name='author', content='Coderthemes')
    link(rel='shortcut icon', href='assets/images/favicon.ico')
    title Evalua #{title}
    link(href='https://fonts.googleapis.com/css?family=Lora:400,400i%7CPoppins:300,400,500,600', rel='stylesheet')
    link(rel='stylesheet', href='assets/css/styles.css')
    link(rel='stylesheet', href='assets/css/us.woocommerce.css')
    link( id= 'theme', rel='stylesheet', href='assets/css/themes/theme-investments.css')
    script(src='/socket.io/socket.io.js')
    script(src='main.js')
  body
    include partials/header
    .container
      .row
        include partials/flash
    block content
  footer
    //include partials/footer
    script(src='assets/js/jquery-1.12.3.min.js')
    script(src='assets/js/plugins.js')
    script(src='assets/js/core.js')
    // Google Analytics: change UA-XXXXX-X to be your site's ID
    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-XXXXX-X', 'auto');
      ga('send', 'pageview');

    script.
      var $calculatorExample2 = $('#calculatorExample2');
      function calculatorExample2() {
      var ageNumber = 0,
      basePrice = 0,
      calcRate;
      if($('#item01').is(':checked')) calcRate = 1;
      else calcRate = 0;
      if($('#item03').is(':checked')) basePrice += 2;
      if($('#item04').is(':checked')) basePrice += 1;
      if($('#item06').is(':checked')) basePrice += 1;
      if($('#item08').is(':checked')) basePrice += 1;
      if($('#item10').is(':checked')) basePrice += 5;
      if($('#item11').is(':checked')) basePrice += 4;
      if($('#item12').is(':checked')) basePrice += 3;
      if($('#item13').is(':checked')) basePrice += 2;
      if($('#item14').is(':checked')) basePrice += 1;
      if($('#item15').is(':checked')) basePrice += 2;
      var result = basePrice + calcRate;;
      $('#formResult > span').html(result.toFixed(2));
      }
      calculatorExample2();
      $calculatorExample2.find('input').on('change', function(){
      calculatorExample2();
      });


    
